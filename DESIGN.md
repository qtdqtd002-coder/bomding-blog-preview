---
name: 쓰담 운영 허브 (Sseudam Ops Hub)
description: 게임·IT 블로그 작성·검수·발행을 한눈에 관리하는 쓰담의 내부 운영 대시보드
colors:
  paper: "#F2F0EC"
  paper-2: "#E9E6E0"
  surface: "#FFFFFF"
  surface-2: "#F6F4F1"
  surface-3: "#EDEAE5"
  ink: "#1A1815"
  ink-2: "#514B44"
  ink-3: "#6F6961"
  line: "#E2DED7"
  line-2: "#CFC9C0"
  line-3: "#B6AFA4"
  accent: "#C4482A"
  accent-hover: "#A93A20"
  accent-soft: "#FBEAE4"
  on-accent: "#FFFFFF"
  ok: "#15703C"
  warn: "#8A5D08"
  bad: "#B3261E"
  info: "#1B5FA8"
  author-bomding: "#D9603C"
  author-yeongdo: "#2A8574"
  author-gemdeokku: "#7355C9"
  author-yeonbom: "#3F7EC0"
  author-harusari: "#BC3D73"
typography:
  display:
    fontFamily: "Pretendard Variable, Pretendard, -apple-system, system-ui, sans-serif"
    fontSize: "24px"
    fontWeight: 800
    lineHeight: 1.25
    letterSpacing: "-0.03em"
  panel:
    fontFamily: "Pretendard Variable, Pretendard, sans-serif"
    fontSize: "16.5px"
    fontWeight: 700
    lineHeight: 1.35
    letterSpacing: "-0.022em"
  row:
    fontFamily: "Pretendard Variable, Pretendard, sans-serif"
    fontSize: "15.5px"
    fontWeight: 600
    lineHeight: 1.5
    letterSpacing: "-0.018em"
  body:
    fontFamily: "Pretendard Variable, Pretendard, sans-serif"
    fontSize: "15.5px"
    fontWeight: 400
    lineHeight: 1.72
    letterSpacing: "normal"
  label:
    fontFamily: "Pretendard Variable, Pretendard, sans-serif"
    fontSize: "13px"
    fontWeight: 600
    lineHeight: 1.4
    letterSpacing: "-0.01em"
  metric:
    fontFamily: "JetBrains Mono, ui-monospace, monospace"
    fontSize: "30px"
    fontWeight: 700
    lineHeight: 1.1
    letterSpacing: "-0.03em"
rounded:
  xs: "6px"
  sm: "8px"
  md: "12px"
  lg: "16px"
  xl: "20px"
  pill: "999px"
spacing:
  xs: "4px"
  sm: "8px"
  md: "12px"
  lg: "20px"
  xl: "28px"
components:
  panel:
    backgroundColor: "{colors.surface}"
    borderColor: "{colors.line}"
    rounded: "{rounded.lg}"
    padding: "16px 20px 18px"
  button-default:
    backgroundColor: "{colors.surface}"
    textColor: "{colors.ink-2}"
    borderColor: "{colors.line-2}"
    rounded: "{rounded.sm}"
    height: "36px"
    padding: "0 14px"
  button-primary:
    backgroundColor: "{colors.ink}"
    textColor: "{colors.paper}"
    rounded: "{rounded.sm}"
    height: "38px"
    padding: "0 15px"
  button-primary-hover:
    backgroundColor: "{colors.accent}"
    textColor: "{colors.on-accent}"
  kpi-card:
    backgroundColor: "{colors.surface}"
    borderColor: "{colors.line}"
    rounded: "{rounded.md}"
    padding: "15px 18px 16px"
  row:
    backgroundColor: "{colors.surface}"
    minHeight: "58px"
    padding: "0 20px 0 0"
  chip:
    backgroundColor: "{colors.surface-2}"
    textColor: "{colors.ink-2}"
    borderColor: "{colors.line}"
    rounded: "{rounded.pill}"
    padding: "3px 11px"
---

# Design System: 쓰담 운영 허브 (Sseudam Ops Hub) — v4 「작업실 (The Studio)」

> **버전 이력** — v2 「따뜻한 관제탑」(인디고) → v3 「편집국」(세리프·신문 조판, 2026-08-03) →
> **v4 「작업실」(2026-08-04)** → **★v5 「Signal」(2026-09-03, 현행)** → **v5.1 모션 계층(2026-09-04)**.
> v3는 하루 만에 폐기됐다. 그 이유가 이 문서의 §1에 있다.
> 토큰 실제값 정본은 `index.html`의 `:root` 블록이며, 이 문서는 그와 동기화된 설계 근거다.
>
> **★stale 고지(2026-09-04)** — v5 「Signal」은 탭 2개(작성 글·트렌드)·무채색 크롬(색은 작성자·데스크 «정체성»에만)·이중 베젤로 전면 개편되며
> **다크모드·관리자·앱 설치·스플래시·따뜻한 종이톤·테라코타 브랜드색을 폐기**했다. 따라서 위 frontmatter 토큰과 §1~§5 의 색·다크모드·
> 세그먼트·KPI·스플래시·SFX 서술은 v4 기준으로 **낡았다** — 현행 색·반경·그림자는 `index.html` 상단 주석 + `:root` 가 정본이고,
> v4 의 **원칙 5(면으로 나눔·라벨 ≥12.5px·1920×1080·버튼 36~38px·여백)와 Named Rules·Do/Don't 는 v5 에도 그대로 유효**하다.
> 모션은 §5 Motion 이 v5.1 로 갱신돼 현행이다. 전면 재작성은 별건.

## 1. Overview

**Creative North Star: "잘 정돈된 작업실 (The Studio)"**

쓰담 운영 허브는 봄딩·영도·겜더쿠·연봄·하루살이 다섯 작성자의 글이 지금 어디쯤 있는지 —
확인 대기인지, 발행됐는지, 큐에 밀려 있는지 — 를 열자마자 파악하고 곧장 다음 행동으로 넘어가는 도구다.
매일 여러 번 여는 화면이므로 **눈이 편하고, 경계가 분명하고, 누를 것이 눌러 보이는 것**이 미학보다 앞선다.

**v3에서 무엇이 틀렸나 (이 시스템의 존재 이유)**

v3는 "편집국"이라는 은유를 택해 세리프 헤드라인 + 1px 룰/점선 + 10px 모노 마이크로라벨로 신문 조판을 흉내 냈다.
제안서와 시안 단계에서는 독창적이고 브랜드에 맞아 보였다. 그러나 실제로 매일 쓰자 세 가지가 무너졌다.

1. **영역 경계가 안 보였다** — 얇은 선과 점선만으로 나누니 블록이 서로 흘러 붙었다.
2. **버튼이 버튼으로 안 보였다** — 텍스트에 가까운 링크형 컨트롤이라 클릭 대상이 불분명했다.
3. **글씨가 눈에 안 들어왔다** — 10~12px 모노 라벨과 세리프 본문이 한국어 UI에서 읽히지 않았다.

사용자의 표현 그대로 **"지저분한 신문기사"** 였다. 교훈은 하나다 —
**컨셉이 강할수록 "이 은유가 사용자의 실제 과업을 돕는가"를 따로 검증해야 한다. 시안 승인 ≠ 사용성 검증.**

**v4의 다섯 원칙**

1. **영역은 '선'이 아니라 '면'으로 나눈다** — 모든 블록이 패널(배경 + 테두리 + 여백 + 미세 그림자)을 갖는다.
2. **읽히는 게 먼저다** — Pretendard 단일, 라벨은 12.5px 이상, 모노는 숫자 전용.
3. **1920×1080이 기준 해상도다** — `--maxw:1560px`. 2컬럼을 장식이 아니라 실제로 쓴다.
4. **버튼은 버튼처럼** — 높이 36~38px 고정, 면·테두리·상태 전환이 분명하게.
5. **밀도는 낮추고 여백을 준다** — 정보를 줄이지 않되 숨 쉴 자리를 만든다.

**Key Characteristics**
- 라이트/다크 양면, WCAG AA 대비 보장
- 따뜻한 뉴트럴 배경(`#F2F0EC`) 위에 **순백 패널**을 띄워 계층을 만든다
- 작성자 색은 출처 표시 전용 — 행 좌측 4px 바, 아바타 링, 칩 채움
- 테라코타 액센트(`#C4482A`) 하나로 브랜드 온기를 유지
- 진입 스태거·KPI 카운트업 등 최소한의 동적 UI (전부 `prefers-reduced-motion` 대체 경로 보유)

## 2. Colors

배경과 패널의 **밝기 차이**가 이 시스템의 첫 번째 도구다. 배경(`#F2F0EC`)이 패널(`#FFFFFF`)보다 어둡기 때문에
패널이 떠 보이고, 그것만으로 영역이 나뉜다. 선은 보조일 뿐이다.

### Surface (면 램프)
- **Paper** (`#F2F0EC`): 앱 배경. 패널보다 반드시 어둡다.
- **Paper Deep** (`#E9E6E0`): 더 깊은 구분 띠.
- **Surface** (`#FFFFFF`): 패널·행·카드.
- **Surface Inset** (`#F6F4F1`) / **Inset Hover** (`#EDEAE5`): 칩·인셋·행 hover.

### Ink (텍스트 램프)
- **Ink** (`#1A1815`): 제목·행 제목·본문 기본.
- **Ink 2** (`#514B44`): 보조 텍스트·칩 텍스트.
- **Ink 3** (`#6F6961`): 메타·라벨·비활성. *종이 위 4.8:1로 AA 통과.*

### Line
- **Line** (`#E2DED7`) 기본 구획 / **Line 2** (`#CFC9C0`) 버튼·입력 / **Line 3** (`#B6AFA4`) hover 강조.

### Accent — 쓰담 테라코타
- **Accent** (`#C4482A`): 브랜드 온기의 유일한 운반체. 종이 위 **4.9:1**, 흰 글자 위 **5.2:1** — 텍스트·채움 양쪽 AA.
- **Accent Hover** (`#A93A20`) / **Accent Soft** (`#FBEAE4`): 눌림·옅은 강조면.

### Status
- **OK** (`#15703C`) 발행 완료 / **Warn** (`#8A5D08`) 검수·진행 / **Bad** (`#B3261E`) 실패·삭제 / **Info** (`#1B5FA8`) 신규·대기.

### Author Identity (출처 전용)
- 봄딩 **테라코타** `#D9603C` · 영도 **틸** `#2A8574` · 겜더쿠 **바이올렛** `#7355C9` ·
  연봄 **블루** `#3F7EC0` · 하루살이 **마젠타** `#BC3D73`

### Topic Desk Category (주제 분류 전용 — 2026-08-14 신설 · 08-15 5분류로 확장)
트렌드 탭의 일간 토픽 데스크가 쓰는 **네 번째 색 축**이다. 작성자 색과 목적이 다르다 —
저건 "누가 썼나", 이건 "무슨 종류의 소식인가".

- 주로 다루는 게임 `--c-core` `#9C3F6E` · 신작 게임 `--c-new` `#1B5FA8` · 최근 업데이트 `--c-upd` `#15703C` ·
  최근 화제 `--c-hot` `#C4482A` · 콘솔 게임 `--c-con` `#5B4B8A`

**왜 새 축을 허용했나.** 다섯 중 셋은 이미 대비 검증을 마친 기존 토큰(`info`·`ok`·`accent`)을 그대로 재사용하므로
실제로 늘어난 색은 **콘솔 바이올렛과 주력 마젠타 둘뿐**이다
(콘솔 종이 위 6.5:1 / 다크 `#A99AE0` 7.6:1 · 주력 종이 위 5.5:1 / 다크 `#E58AB4` 7.8:1 — 전부 AA).

**마젠타 주의.** `--c-core` `#9C3F6E` 는 하루살이 `#BC3D73` 과 계열이 가깝다. 지금은 충돌하지 않는다 —
`core` 는 `core-games.cjs` 가 **봄딩·영도만** 대상으로 산출하므로 하루살이가 이 분류의 레인으로 나올 수 없다.
★하루살이(또는 다른 마젠타 계열 작성자)를 `core` 대상에 넣게 되면 **이 색을 먼저 바꾼다.**

**The Category-Never-Alone Rule.** 분류는 **색만으로 전달하지 않는다.**
섹션 헤더는 항상 `아이콘 + 한글 라벨 + 건수`를 함께 띄우고, 행의 분류 정보도 목적 태그 텍스트로 중복 표기한다.
작성자 색에 적용한 규칙(색 옆에 항상 이름)을 그대로 따른다 — `core` 행의 레인 칩도 **아바타 + 작성자 이름**을 함께 둔다.

### Topic Desk List (목록 — 2026-08-15, 카드 그리드 폐기)
초판은 카드 그리드였다. 사용자 평가는 **"어떤 게임인지 한눈에 보기가 힘들다"** 였고, 원인이 명확했다 —
카드마다 게임명이 서로 다른 높이에 놓여 **세로로 훑을 축이 없었다.**
→ §Row(목록) 패턴을 따라 **게임명을 고정 열로 세웠다.** 눈이 한 축만 따라가면 되고, 스캔 대상이 곧 게임이 된다.

- 열 구성: `4px 색바 · 168px 게임명(+플랫폼·화제도·레인) · 1fr 주제+왜 · 148px 목적·시의성 · 122px 발주`
- **모든 셀에 `grid-row:1`** — 안 하면 메타가 2행으로 흘러 줄이 깨진다(§Row와 동일한 함정).
- 1240px에서 목적·시의성 열을 접어 본문 아래로 내리고, 960px에서 `grid-template-areas`로 3줄 카드 행이 된다.
- 교훈: **정보를 줄이지 않고 축을 세우는 것**이 스캔 가능성을 만든다. 카드는 정보를 담지만 축을 주지 않는다.

### Named Rules

**The Author-Color Rule.** 작성자 색은 출처 전용이다. 교차하거나 장식으로 전용하지 않는다. 색이 곧 출처다.
단, **색만으로 정보를 전달하지 않는다** — 아바타 옆에 항상 이름 텍스트를 같이 둔다.

**The Theme-Pair Rule.** 면과 글자는 **반드시 같은 테마 쌍**으로 쓴다 —
`background:var(--ink)`에는 `color:var(--paper)`, `background:var(--accent)`에는 `color:var(--on-brand)`.
`color:#fff`처럼 한쪽을 고정값으로 박으면 다크에서 `--ink`가 밝아지는 순간 **흰 배경 위 흰 글자**가 된다.
(v4 개발 중 실제로 처방 라벨이 이 방식으로 완전히 사라졌다.)

**The Surface-Gap Rule.** 배경과 패널은 항상 다른 밝기여야 한다. 같아지면 영역 구분이 즉시 무너진다.

## 3. Typography

**단일 폰트: Pretendard Variable** (fallback: Pretendard, -apple-system, "Apple SD Gothic Neo", system-ui)
**수치 전용: JetBrains Mono** — KPI·카운트·읽기 시간·날짜에만. 한글에는 절대 쓰지 않는다.

### Hierarchy
- **Display** (800, 24px, ls -0.03em): 뷰 제목(`.viewhead h1`).
- **Panel** (700, 16.5px, ls -0.022em): 패널 제목(`.panel-h h2`).
- **Row** (600, 15.5px, ls -0.018em): 목록 행 제목(`.c-title`) — 이 화면에서 가장 많이 읽는 텍스트.
- **Body** (400, 15.5px, lh 1.72): 본문·설명.
- **Label** (600, 13px): 칩·버튼·메타 라벨.
- **Metric** (Mono 700, 30px): KPI 숫자. 서고 카운트는 38px.

### Named Rules

**The Readable-Floor Rule.** UI 라벨의 하한은 **12.5px**이다. 그 아래로 내려가면 한국어에서 읽히지 않는다.
v3는 10~11px 모노 라벨을 썼고 그게 "눈에 안 들어온다"의 직접 원인이었다.

**The Mono-For-Numbers-Only Rule.** 모노스페이스는 숫자에만. JetBrains Mono에는 한글 글리프가 없어
한글이 섞이면 시스템 폰트로 폴백해 굴림체처럼 깨진다. 숫자는 `font-feature-settings:"tnum"`으로 자릿수 정렬.

**The One-Family Rule.** 폰트는 Pretendard 하나. 위계는 웨이트(400–800)와 크기로만. 세리프를 다시 들이지 않는다.

## 4. Elevation

깊이는 **면의 밝기 차 → 1px 테두리 → 미세 그림자** 순으로 만든다. 그림자는 마지막 보조 수단이다.

### Shadow Vocabulary
- **xs** (`0 1px 2px rgba(52,42,30,.05)`): 패널·카드 기본. 거의 안 보이지만 종이가 떠 있다는 신호.
- **sm** (`0 1px 3px + 0 1px 2px`): 세그먼트의 선택된 탭, hover한 행 카드.
- **md** (`0 4px 14px + 0 2px 5px`): hover로 들어올려진 KPI·클러스터·갤러리 카드.
- **lg** (`0 16px 40px rgba(40,30,18,.16)`): FAB·서고 블록 등 떠 있는 요소.
- **ring** (`0 0 0 3px accent-soft`): 입력 포커스.

그림자 색은 회색이 아니라 **따뜻한 갈색 계열**(`rgba(52,42,30,…)`)이다. 종이 온도를 유지하기 위해서다.

## 5. Components

### Panel (시그니처)
- 흰 면 + 1px `line` + 16px 라운드 + `sh-xs`. 헤더(`.panel-h`)·본문(`.panel-b`)·푸터(`.panel-f`) 3부 구조.
- 헤더: 제목(16.5px/700) + 카운트 칩 + 우측 액션 버튼, 하단 1px 구분선.
- 본문이 목록일 때는 `.panel-b.flush`로 패딩을 없애 행이 패널 가장자리까지 닿게 한다.

### KPI Card
- `flex:1 1 160px`으로 가로를 균등 분할. **`auto-fit` 그리드를 쓰지 않는다** — 숨긴 셀이 섞이면 폭을 못 채운다.
- 숫자(Mono 30px) 위, 라벨(13px/600) 아래. hover 시 하단 3px 바가 좌→우로 자란다.
- **실측값만 표시한다.** 백엔드가 주지 않는 수치는 셀을 숨기고 "미연결"이라 적는다. 0으로 위장하지 않는다.

### Buttons
- **Default** (`.btn`/`.sbtn`): 높이 36px, 흰 면 + `line-2` 테두리. hover 시 `surface-2` + `line-3`.
- **Primary** (`.app-cta`/`.rf-submit`/`.fab`): `ink` 면 + `paper` 글자. hover 시 **accent로 전환** + 1~3px 상승.
- **Icon** (`.icon-btn`): 38×38 정사각, 테두리 유지.
- **Segment** (`.nav-seg`): 인셋 트랙 안에서 선택된 탭만 흰 면 + `sh-sm`으로 떠오른다.
- **Chip** (`.wchip`/`.rf-chip`): 알약, 선택 시 작성자색 채움(`--ac-fill`) + 흰 글자.

섹션 이동은 **세그먼트**, 작성자 필터는 **칩** — 선택의 성격이 다르므로 형태를 다르게 한다.

### Row (목록)
- `grid-template-columns: 4px 132px 1fr 172px 66px 78px` — 색바·작성자·제목·분류·읽기·상태.
- 높이 58px. **모든 셀에 `grid-row:1`을 명시한다** — 안 하면 메타가 2행으로 흘러 줄이 깨진다.
- 좌측 4px 작성자 색 바는 평소 `opacity:.55`, hover 시 1.0.
- 1100px 이하에서 분류 칩을 접고, 720px 이하에서 2줄 카드 행(`grid-template-areas`)으로 재배치한다.

### Inputs
- 흰 면 + `line-2` 테두리 + 8px 라운드, 패딩 11×13. focus 시 accent 테두리 + `sh-ring`.
- 검색 입력은 focus 시 250→290px로 넓어진다.
- ★**v5 현행 포커스 링(2026-09-06 사용자 지시)**: 모든 입력(`.f-i`·`select.f-i`·`.pin-i`·`.fsearch:focus-within`·도구함 숫자칸 `.th-nm input`)은 focus 에 **`box-shadow:0 0 0 2px var(--ink)`**. 종전 1px `hair-3` 는 대비 1.6:1 로 WCAG 1.4.11(3:1) 미달이었다(도구함 v2.1 게이트에서 실측).

### Home — 「홈」 탭 벤토 (2026-09-04 신설 · 사이트의 대문)
탭 순서 **홈 · 작성 글 · 트렌드**, 기본 진입은 홈. 12열 벤토 **3타일(5+7 / 12)**, 860px 이하 단일 열. 타일은 전부 이중 베젤(`.tray > .core`)이며 같은 줄의 타일은 코어가 트레이를 채워 키를 맞춘다.
★**09-05 2차 개편(사용자 결정)** — 잔여 사용량은 타일에서 빠져 **상단 고정 칩**(종 왼쪽, 모든 탭)이 됐고, 주간 사용량+일간 발행은 **「사용량」 한 차트**로 합쳤으며, 일별 방문자 타일은 뺐다(로거는 계속 기록). 남은 타일 = 작업 중 · 사용량 · 블로그 활동.
★**09-06 개편(사용자 지시)** — 「AI 브리핑 인용」을 **「블로그 활동」**으로 개명하고, 같은 타일에 **일자별 글 작성 수(꺾은선)·월 단위 총 글 수**를 넣었다. 인용이 비공개인 작성자(영도)도 이제 빈 타일이 아니라 자기 활동을 보인다.

| 타일 | 출처 | 형태(dataviz 규칙) |
|---|---|---|
| **작업 중** (5) | 백엔드 `/requests` received·processing | 목록 행(작성자 점·주제·상태 펄스·경과). 푸터=오늘 발행·실패·마지막 발행. 1분마다 재조회, 탭 복귀 시 재조회. 빈 배열≠미연결(`REQS_OK`) |
| **사용량** (7) — 09-05 통합 · **3차 콤보** | 토큰=`_trend/_usage.json`(하루 1회 스냅샷·추정) · 편수=`/requests` 종결 상태(실시간·1분 재조회) | 스탯 4칸(오늘 발행·7일 발행·7일 실패율·7일 토큰) → 14일 **콤보 차트**(사용자 결정 — «막대 아래 숫자 행»은 읽기 어려워 폐기): 작성자 스택 막대(**왼쪽 축 토큰**, ≤24px·2px 간격·헤어라인) + **발행 편수 = 잉크 선 2px+점 3.5px(표면 링)**·**실패·건너뜀 = 경고색 점**(**오른쪽 축 편**, 눈금 최소 4). 축 단위 «토큰»·«편»을 축 머리에. 선·점은 포인터를 통과시켜 hit 은 막대 슬롯 그대로. 캡 라벨=최대·오늘만, 범례=작성자(사각)+발행(선 키)+실패(점 키), 툴팁 한 개에 토큰·편수 전부, 표 뷰. 미연결이면 오른쪽 축·선·점을 그리지 않고 범례 «발행 미연결»·표 «—»(0 아님) |
| **블로그 활동** (12) — 09-05 신설(구 「AI 브리핑 인용」) · ★**09-06 개명·글 축 추가** | `_trend/_citations.json`(`_tools/mate-citations.cjs`) — 인용·방문자=네이버 공개 프로필 일별 스냅샷 · **글 수=글 목록 `addDate` 를 KST 달력일로 센 값**(누적 postCount 차분이 아니라 실제 글 날짜라 백필·삭제에 안 흔들린다) | **작성자 2칸**(`.cr-grid` 봄딩 ｜ 영도, 헤어라인 세로 구분 · 860px 이하 세로 스택): 점+이름+«9월 게임 메이트» 칩 → 스탯 **두 줄**(인용 4칸=어제 증분·이달·기준월·누적 / **글 4칸=어제 글·이달 글·지난달 글·총 글**, 모노 숫자) → 14일 차트. **글 작성 수는 항상 꺾은선**(사용자 지시 09-06): 인용이 공개면 «인용 막대(왼쪽 축·작성자 색) + 글 선·점(오른쪽 축 «편»·잉크)» 콤보, 비공개면 왼쪽 축을 «편» 으로 써서 **작성자 색 선 하나**. 0 편인 날도 0 으로 찍는다(빈칸=모름과 구분). 캡 라벨=최대·마지막만, 툴팁=인용·글·누적·스냅샷 시각, 범례=인용(사각)+글(선 키), 표 뷰. **인용은 어제까지만 값이 있고 오늘은 «집계 전»**. 인용수는 메이트 선정 블로그만 공개라 영도는 «인용수 비공개 · 메이트 선정 시 자동 공개» 한 줄만 두고 글 축은 그대로 그린다(0 으로 위장하지 않는다). 지난달 칸의 `*` = 그 달을 창(≈62일) 시작 이후만 덮었다는 표시 |
| ~~잔여 사용량~~ → **상단 고정 칩 `.qchip`** — 09-05 | `_trend/_quota.json`(`_tools/quota.cjs`) | 종(`.bell`) 왼쪽 `right:74px` 44px 유리 칩, 모든 탭에서 보임: 게이지 아이콘 + «남음» + `5시간 72%` · `7일 87%` 둘뿐. 남음 10~15% 면 그 숫자만 경고색, **<10% 면 칩 전체가 경고색(흰 글자)+아이콘 ⚠+«거의 소진»**(3차). 상세는 **칩 바로 아래 고정 패널 `.qpop`**(`top:70px right:74px` 폭 344px 고정, 3열 그리드 창·남음·초기화, **줄바꿈 없음·마우스 위치 무관** — 3차: 마우스를 따라다니는 `.ctip` 은 폭이 변해 강제 개행됐다) — 경고 시 마지막 줄에 «○○ 사용량이 거의 소진됐어요 · n% 남음 · 초기화». hover·focus 동일. 스냅샷 없으면 칩을 숨긴다(0 위장 금지). 모바일(≤860)은 섬이 전폭이라 칩 `top:66px right:12px` 34px, 패널 `top:106px` |
| ~~일간 발행~~ · ~~일별 방문자~~ | — | 09-05 폐기(사용자 결정) — 일간 발행은 「사용량」 편수 행으로 흡수, 일별 방문자 타일은 제거(로거는 `_citations.json` 에 방문자를 계속 기록해 둔다) |

- **과도기 상태(09-05 · 09-06 갱신)**: 두 타일은 «한쪽만 있음·전무»를 정상 상태로 그린다 — 인용도 글도 없으면 «일별 집계 전 · 스냅샷 N개»(인용만 없으면 글 축으로 그린다), 인용수 비공개(메이트 선정 전)면 «인용수 비공개 · 메이트 선정 시 자동 공개» 1줄. 빈 자리를 0이나 장식으로 채우지 않는다. 키보드 포커스 툴팁은 **플롯 «아래»**(축 라벨·범례 위)에 붙는다 — 위에 올리면 타일 헤더와, 옆에 붙이면 이웃 열과 겹친다(09-05 겹침 수정 최종본. ★문서가 «옆»으로 낡아 있던 것을 09-06 게이트 🟡5 에서 코드 실측으로 정정).
- **색 검증(09-05 추가)**: 봄딩 `#C43D63` ↔ 영도 `#0F7C86` 쌍 — CVD ΔE 8.7·정상시 26.0·대비 ≥3:1 PASS, 영도 채도(0.089) 만 «회색으로 읽힘» FAIL → 정체성 색이라 유지하고 2차 인코딩(끝 라벨·범례·툴팁·표 뷰)으로 보완. «한 축» 규칙의 **의도적 예외(사용자 결정 09-05 3차)**: 「사용량」은 토큰 막대+편수 선의 **콤보 차트(이중 축)** 다 — 막대 아래 숫자 행이 읽기 어렵다는 실사용 평가가 규칙보다 앞선다. 예외를 읽히게 하는 장치 = 축 머리 단위(«토큰»·«편»), 선은 잉크 단색, 범례 키를 마크와 같은 모양(사각/선/점)으로.
- **색 검증(dataviz validator)**: 작성자 5색은 정체성 정본이라 바꾸지 않는다. 검증기 결과 인접쌍 CVD ΔE 4.4(연봄↔하루살이)·회색 채도 미달 → **2차 인코딩으로 보완**(2px 간격·항상 범례·툴팁·표 뷰). 텍스트는 절대 시리즈 색을 입지 않는다.
- **차트 라벨 클램프(09-06 게이트 🔴 조치)**: 캡 라벨(최대·마지막 값)의 x 는 `capX()` 로 플롯 안(`padL` ~ `W-padR`)에 가둔다 — 폭 어림은 11px 모노 기준 한글 11px·그 외 6.6px. 안 가두면 **맨 오른쪽 칸의 캡이 오른쪽 축 눈금·단위 «편» 과 겹친다**(차트 실폭 ≤384px = 뷰포트 ≤460px, 즉 폰 전 구간에서 실측). 선 위 캡도 같은 규칙.
- **점 크기는 하나(3.5px)**: 0 편인 날도 같은 크기로 찍는다 — «크기»를 값에 따라 바꾸면 문서에 없는 2차 인코딩이 생긴다(09-06 게이트 🟡3). 「사용량」의 발행 선은 0 인 날 점을 아예 안 그려 관례가 다르다 — **알고 남긴 차이**(블로그 활동은 «그날 0편»이 뜻 있는 값이고, 발행 편수는 백엔드 미연결과 0 을 구분해야 한다). 맞출 거면 두 타일을 같이 손본다.
- **모바일 hit 폭**: 360px 에서 차트 열 하나(키보드 포커스·터치 대상)가 슬롯 15~17px 로 40px 권장치에 못 미친다 — 「사용량」에도 있는 기존 패턴이라 오늘은 손대지 않았다(09-06 게이트 🟡7). 손댈 땐 두 타일을 같이 본다.
- **끝 날짜가 「사용량」과 다르다**: 「사용량」은 오늘까지, 「블로그 활동」은 **어제까지**다 — 인용 증분이 «다음 스냅샷과의 차»라 오늘 값은 내일 아침에야 생긴다. 오늘 칸을 «집계 전» 자리로 그려 축 끝을 맞추는 안은 보류(빈 자리를 장식으로 채우지 않는다, 09-06 게이트 🟡6).
- **차트 모션**: 막대는 기준선에서 자라고(scaleY), 게이지 호는 그려지고, 숫자는 굴러간다 — 전부 `MO.chart`. 마크업에 최종값이 있어 GSAP 없이도 정적 완성. 새 데이터가 오면 타일 코어만 제자리 교체(전체 재애니메이션 금지).
- 스냅샷 생산 = **데스크 [E](매일 06:00) 하루 1회뿐**(사용자 결정 09-05 — 드레이너 발행마다 갱신은 철회). 타일 머리는 «n분 전»이 아니라 스냅샷 시각(`06:12 기준`, 다른 날이면 `9.4 06:12 기준`)을 보인다. ★**09-06 수정** — 06:00 에 로컬 OAuth 토큰이 만료돼 있으면 `quota.cjs` 가 그냥 보류하는 바람에 **09-04 이후 한 번도 갱신되지 않았다**(칩이 이틀 묵은 값을 보이고 있었다). 이제 만료면 `claude mcp list`(LLM 0·3초)로 CLI 에게 정규 갱신을 시킨 뒤 다시 읽는다. 그래도 만료면 예전처럼 보류(exit 0)하고 마지막 값을 그 시각과 함께 보여 준다 — 0으로 위장하지 않는다. «작업 중»과 「사용량」의 발행 편수는 스냅샷이 아니라 백엔드 실시간(1분 재조회)이다.

### Request Modal — 새 글 요청·발주 요청 (2026-09-05 정리)
- **칸 4개뿐**: 주제 · 요청 사항(텍스트 영역 6줄 = `min-height:164px`, 스크롤 없이) · 작성자 · 요청 문서. 「글의 목적」은 묻지 않는다(주제에 담긴다 — 백엔드 `purpose=null`, 러너가 추정. 트렌드 발주만 데스크 purpose 를 조용히 동봉). 절 표기(필수/선택)·헤더 부제·작성자 안내문·첨부 안내 3줄 전부 제거 — 남는 문구는 라벨 4개 + 드롭존 안 두 줄(«파일을 여기에 놓거나 클릭» / «docx xlsx pdf txt hwpx · 파일당 20MB · 최대 5개 · 이 요청 1건에만 최우선 규칙으로 적용»)뿐.
- **요청 문서 드롭존 `.dz`**(09-05, 2차): 면(`surface-2`)+**1.5px 점선**(`hair-3`) 사각 영역, `min-height:140px`. 클릭·Enter/Space → 파일 선택창(`multiple`), 드래그 앤 드롭 → 그 자리에서 형식(docx·xlsx·pdf·txt·hwpx)·**파일당 20MB·최대 5개** 검증. 들어온 파일은 행 목록(체크·파일명·크기·지우기)으로 쌓이고 «N/5 · 더 놓거나 클릭»; 같은 파일은 한 번만. 놓기 직전 `.over` = 실선 잉크 2px·1% 확대·아이콘 2px 상승. 모달 전체에서 드롭 기본 동작(파일 열림) 차단. 백엔드는 같은 한도(`attachments[]`, `attachment`=첫 파일 호환). 게이트 2차(09-05) 반영: 행 간격 10px + «지우기» 확장 히트박스 ±12px(43px 실측, 이웃 행과 안 겹침 — ±10px 은 39px 라 미달) · 모달 본문 캡 `min(72vh,720px)`(요청 사항 6줄+5행=686px 가 1080px 에서 내부 스크롤 없이) · 파일이 들어오면 마지막 행·«N/5» 줄과 풋터를 `scrollIntoView` 로 보이게(모바일은 본문 캡이 없어 판 `.back` 이 스크롤 — 5개 채우면 풋터 14px 가 첫 화면 밖이던 것, 재게이트 09-05) · 폼 placeholder 는 `ink-3`(surface-2 위 AA, `ink-4` 는 4.47:1 미달).
- **요청 사항 플레이스홀더** = 채우는 법이 보이는 예시 5줄(«예: … / - 꼭 넣기 / - 참고 / - 톤 / - 빼기»).
- **작성자는 사람이 고른다**: 「자동 배정」 없음, 기본 선택 없음. 미선택 전송 → «작성자를 선택해주세요» + 칩 무리 경고 테두리 1.8초 + 첫 칩 포커스. 트렌드 발주는 데스크 lane 이 있으면 기본 선택.
- **요청 문서 = 광고주 가이드**: 1회용·그 글의 최우선 규칙(첨부 > 문체·목적 규칙, 단 발행 게이트 금지요소·공식 사실 충돌은 따르지 않고 failed+사유 환류). 키보드로도 첨부 가능(`.dz` tabindex+Enter/Space).
- **전송 실패는 이유를 그대로**: 백엔드 거절(형식·크기·휴면 작성자)은 `sendfail` 로 그 문구를 보여 준다. 임시 저장(`queued`)은 네트워크 오류일 때만.
- 탭 알약: 숫자가 없는 탭은 `.n:empty{display:none}` — 빈 칸이 gap 을 차지해 글자가 왼쪽으로 밀리던 문제(09-05).

### Toolbox — 「도구함」 탭 (2026-09-06 신설 · 글쓰기 도구 모음)
탭 순서 **홈 · 작성 글 · 트렌드 · 도구함**. 좌측 **도구 목록**(232px 트레이, 행 = **아이콘 타일 + 이름**뿐 — 한 줄 부제는 09-06 사용자 지시로 폐기(§Do/Don't 설명문 금지), 선택 = 잉크 채움 — 탭 알약·칩과 같은 어휘) + 우측 **도구 판**(트레이 코어, `min-height:440px`). 860px 이하는 목록이 가로 한 줄(스크롤)로 접힌다. ★**09-07 넓은 작업 영역 + 사이드 레일**(사용자 지시 «사이드 메뉴는 더 왼쪽으로, 진짜 사이드 메뉴탭처럼» → 3차 «작업 영역은 1920×1080 기준으로 넓혀서»): 도구함 뷰에서만 `render()` 가 `.main` 에 `.wide`(max-width **1620px**)를 붙인다 — 홈·작성 글·트렌드는 1080 그대로. 목록은 그 안 왼쪽 **sticky 레일**(232 · `top:84px`, ≥861), 판이 나머지(1920 에서 1326 · 1366 에서 1072). 처음(오전) 안이던 «본문 열 왼쪽 여백에 −246 마진으로 매달기»는 판이 1032 에 갇혀 보드가 실제 크기(693)로 못 보여 폐기.
- ★**도구 파일을 고치면 `?v=` 를 같은 커밋에서 올린다**(09-07 신설 게이트) — 안 올리면 라이브 파일만 새것이고 브라우저는 같은 URL 을 캐시(`max-age=600`)에서 꺼내 써서 «고쳤는데 화면은 그대로»가 된다(09-07 두 번 발생, 사용자가 먼저 발견). `pre-push` 가 «이번 push 에 `_toolbox/*.js` 가 담겼는데 index.html 의 그 도구 `?v=` 가 안 바뀜»을 차단한다. 서비스워커가 네트워크 우선이어도 **브라우저 HTTP 캐시는 그 앞단**이라 소용없다.
- **도구는 파일 단위**: `_toolbox/<id>.js` 가 `window.SseudamTools[id] = {mount(host, api), unmount()}` 를 등록하고, 목록에서 처음 고를 때만 받는다(`TOOLS[]` 항목 = `{id, name, icon, src}` — 부제 필드 없음)(`?v=날짜` 캐시 버스트, 실패 시 «다시 시도»). `api` = 사이트가 빌려주는 부품(openModal·closeModal·toast·MO·svg·esc·icon) — 도구는 사이트 토큰만 쓰고 색을 새로 만들지 않는다. 탭을 떠나면 `unmount()`(문서 단위 리스너 해제), 상태는 모듈 메모리에 남아 복귀하면 그대로 복원된다.
- **썸네일 제작**(목록 표시명 · 09-06) `_toolbox/thumb.js` + 미리보기 자산 `_toolbox/thumb/`(≈140KB). 규격 = **봄딩 썸네일 표준 v1**(2026-09-05, `쓰담/docs/…/썸네일표준/apply-thumb-standard.py` 를 그대로 이식) — 1000×1000 · 글자 높이 16.5% · 안전여백 4% · 테두리 없음. **★v2(09-06 사용자 피드백 8건)**: 부제 입력칸은 항상 보이고 **비우면 그리지 않는다**(입력하면 «90px에서 읽히지 않음» 경고 2곳 — 표준은 부제 없음) · 부제는 **타이틀 → 밑줄 → 부제** 순으로 아래에 · «글 제목» 칸 제거(미리보기 카드 제목은 타이틀+부제). **디자인 10** = 밴드(표준) · 상단 밴드 · 플라크 · 틴트 · 스트라이프 · 하프 · 크림 · 글래스(캔버스 blur) · 스트로크(면 없이 플럼 외곽선+그림자) · 모노(흑백+포인트색 글자) — 열 다 글자 높이·여백은 같고 구조만 다르다. **면 진하기** = 슬라이더 + **숫자 입력**(15~100%, ★v2.1 기본 **90%** — 사용자 지시 09-06; 저장 키 `sseudam_thumb_v3`) 이 밴드 알파 곡선·판·띠의 알파를 곱한다(면 없는 스트로크는 슬라이더·숫자 둘 다 비활성). **글자 크기**(v2.1) = 타이틀·부제 각 입력칸 아래 «크기» 줄(슬라이더+숫자, 캔버스 % — 타이틀 8~24 기본 16.5 / 부제 3~10 기본 5.2), 16% 미만이면 검사 목록에 «표준 16.5% 미만». 슬라이더↔숫자는 어느 쪽을 만져도 따라오고, 숫자는 타자 중 범위 안일 때만 반영·blur/Enter 에 범위로 정정. 설정 열이 2줄 늘어 간격을 줄였다(섹션 12/14·라벨 10/5·타일 68px) — 부제 경고까지 뜬 가장 긴 상태에서도 1080px 안. **포인트 색** = 봄딩 3(로즈 `#C93C7C`·핑크 `#F58AB4`·플럼 `#2E2038` — 라이브 블로그 `.pcol3`/`.pcol1` + 표준 밑줄색) · 영도 3(그린 `#15A05A`·민트 `#14B8A6` — 영도 output-format §1-4 핵심색 · 틸 `#0F7C86` — 사이트 정체성색) · **기타** = 브라우저 팔레트(`<input type=color>`, 고른 색이 스와치에 남는다). **글자색** = 자동(디자인이 정함: 어두운 면 흰색·크림/밝은 띠 플럼·모노 포인트색) · 흰색 · 플럼 · 크림 · **하늘 `#58CCFF`**(09-07 사용자 지정) · 기타(팔레트). ★**HEX 직접 입력**(v2.2 · 09-07 사용자 지시 — 팔레트는 OS 대화상자라 RGB 칸뿐이다): 스와치 줄 밑 «이름 + HEX 칸»(`#thHex`·`#thTcHex`, 100×28 모노)에 `#abc`·`abc`·`C93C7C` 를 치면 `#RRGGBB` 로 정규화해 **읽히는 순간 반영**하고 «기타»로 전환한다 — 스와치·팔레트·칸 3방향 동기, **타자 중(focus)에는 값을 덮지 않는다**. 못 읽는 값은 칸을 떠날 때 이전 값으로 되돌리고 붉은 링 900ms. 글자색이 «자동»이면 칸은 비고 placeholder 가 «자동». 이 두 줄이 늘어 간격을 한 번 더 회수했다(섹션 10/12 · `.th-crow` 32 · `.th-swn` margin 0 · 마지막 섹션 bottom 4) — **1080 여유는 3px 뿐이라 이 열에 줄을 더할 땐 반드시 재측정**. **글꼴 6** = Pretendard 800 · 검은고딕 · 도현 · 주아 · 송명(세리프) · 맑은 고딕 — 구글 폰트 4종은 마운트 때 `<link>` 한 번, 캔버스는 `document.fonts.load` 뒤 다시 그린다. 이미지 = 드롭·클릭·**Ctrl+V**, 확대 1~3×(슬라이더·휠·+/−)·드래그/화살표 이동·가운데. 왼쪽 판 = 캔버스(1000, 표시 ≤400px) + **실제 크기 미니 2**(메인 목록 183×185 · 프롤로그 90×90, 절반씩 단계 축소) + **검사 3**(원본 해상도 ≥1000 · 타이틀 ≤6자 · 부제 없음) + **내보내기**(v2 에서 설정 열 → 캔버스 아래로: 설정이 길어져 1080px 화면에서 CTA 가 밀리던 것) = **PNG 저장**(`<타이틀>_1000.png`) · **클립보드 복사**(네이버 에디터에 붙여넣기). 설정(디자인·색·글자색·글꼴·면 진하기·글자 크기)은 `localStorage.sseudam_thumb_v3`, 이미지는 저장하지 않는다.
- **블로그 미리보기 모달** `.mdl.wide`(1060px, `openModal(html, opener, label, 'wide')` — 본문 패딩 0·캡 `min(84vh,960px)`): **PC 프롤로그 966px**(시안 A 배너 966×360 → 메뉴 6칸 → 사이드바 프로필 161 → 메인 목록 큰 카드 183×185 + 목록 90×90 → 「게임」 8열 90×90 = 2026-09-05 네이버 실측 배치·목록 글꼴 돋움 9pt·제목색 #2E2038·강조색 #C93C7C) / **모바일 390px**(m.blog 커버 430·40% 딤 → 프로필 68 원형 → 사진 목록 3열 33.3% `margin:1px; padding-top:100%` = m_main.css 규칙 그대로). 새 썸네일은 **실제 픽셀 크기**로 첫 자리에, 이웃 글은 실제 봄딩 글 5편(제목·표준 썸네일 1:1). 판은 `.th-fit{width:max-content}` 를 `transform:scale` 로 모달 폭에 맞춘다 — 래퍼 `.th-fitw{contain:inline-size}` 가 없으면 966px 자식이 그리드 자동 트랙의 최소 폭을 밀어 모바일에서 모달이 966 으로 벌어진다(09-06 실측).
- **티어표 제작**(2026-09-07 신설 · 오전 v1 「잉크 농도 보드」 → 같은 날 사용자 피드백 10건으로 **v2 「작성자 전용 보드」**) `_toolbox/tier.js`(의존성 0) + 리소스 묶음 `_toolbox/tier/res/` — 블로그 본문용 티어표 **PNG(논리 폭 693 · 높이 자동 · 2배 1386 으로 내보냄)**. 사용자 요구 = «흔히 보는 티어표가 아닌 독창적·직관적 쓰담 전용 양식 → 양산형이 아니라 봄딩·영도 전용 디자인 느낌, 네이버 실제 크기, 캐릭터 일러스트 리소스를 끌어 넣기». **v1 에서 살아남은 핵심 6**: ①**농도 = 등급** — 포인트 색 하나를 `1.0 → 0.14` 등간으로 옅혀 플라크를 채움(디자인 바탕색 위에 실색으로 섞는다 — 범례 없이 «진할수록 강하다». ★플라크 글자색은 **흰색 → 디자인 잉크 → 진한 잉크(봄딩 `#17101E`) → 검정 순으로 4.5:1 을 넘는 첫 후보**(`plaqueText` — 흰·검정 중 하나는 어떤 밝기에서도 4.5 를 넘어 항상 해가 있다). 고정 휘도 임계값 .42 는 두 번째 티어에서 흰 글자 3.05:1(봄딩)·2.36:1(영도)을 내 design-reviewer 🔴(09-07)로 잡혔고, «흰·잉크 중 큰 쪽»만으로는 봄딩 7단계 A `#D1578E` 가 3.96 에 걸려 세 번째 후보를 뒀다. 영도 기본 그린 `#15A05A` 는 흰 글자 3.38:1 이라 S 플라크도 잉크 글자가 된다 — 두 디자인 7단계까지 전 티어 ≥4.5:1 을 게이트가 잰다) ②**초상 타일 + 이름** ③**티어별 «한 줄»** + **바닥 기준 스탬프·서명** ④**세로 보드**(독자=모바일, 검사 목록이 «폰 이름 ≈ 9.9px» 를 693→360(×0.52) 환산으로 표시, 9.5 미만 경고) ⑤이미지 없으면 **모노그램**, 이름도 없으면 **점선 빈 슬롯**(= 양식) ⑥**캔버스에서 끌어 이동**(고스트+잉크 삽입 마커, 클릭=선택, 터치는 탭=선택·이동은 편집 열). **v2 에서 바뀐 것(피드백 ①~⑩)**: ①도구함 뷰의 본문 열을 **1620 으로 넓히고**(`.main.wide`) 목록은 왼쪽 sticky 레일, 판 1326(1920 기준) ②**세로 가운데 정렬** — 플라크 글자+라벨 묶음, 타일 블록(플라크보다 낮으면 가운데), 제목 ③**디자인 2종 = 봄딩 / 영도**(색만 바꾼 리스킨이 아니라 각 작성자 정본에서 뽑은 값: 봄딩 = 나눔스퀘어·플럼 `#2E2038` 제목 판 + 로즈 밑줄(썸네일 표준 모티프)·로즈/핑크/플럼 스와치·바탕 `#FFFBFC`·트레이 핑크·점선 구분·모서리 16/18%·서명 ♥ / 영도 = Pretendard·틸 `#0F7C86` 띠 + 민트 3px 룰·그린/민트/틸 스와치·흰 바탕·쿨 그레이 트레이·실선 구분·모서리 8/12%·서명 ■) — 서명 칸을 비우면 디자인 이름이 들어간다 ④**폭 693 = 네이버 PC 본문 이미지 실폭**(09-07 봄딩 `224402846769`·영도 `224403920929` 실제 글을 헤드리스로 렌더해 실측: `#post-area` 773 · `.se-section` 693 · `.se-image-resource` 693 — 두 블로그 동일 `contw-773`) → 논리 693 으로 그리고 `EXPORT_SCALE=2`(1386) 로 내보낸다. 파일명 `<제목>_티어표.png` ⑤**순위 번호·플라크 개수 삭제** ⑥**포인트 색 = 디자인별 3 + 팔레트 + 헥스 입력**(`#RRGGBB`, 타자 중 유효할 때만 반영·6자 이상 무효면 경고 링·blur 에 유효값으로 정정) · **타일 모양 5** = 둥근 사각(18%/12%)·원·스쿼클(초타원 n=4)·육각(뾰족 위)·세로 카드(폭×1.28) ⑦**작업대** — `.tier{height:calc(100dvh − 214px); min-height:600px}` 그리드 `"stage ctl" / "act ctl"`: 왼쪽 보드 스테이지(`surface-2`, 내부 스크롤, 캔버스 `min(100%,693px)` — 1920 에서 **실제 크기 693**, 1366 에서 688)·오른쪽 편집 열 **400px**(≤1400: 340, 내부 스크롤: 디자인 → 제목·기준·서명 → 티어 → 항목)·보드 아래 **고정 내보내기 줄**(검사 칩 + 복사·JSON·열기·비우기·PNG 저장 + 파일명·크기). 항목이 늘어도 페이지가 길어지지 않는다. ≤860 은 세로 흐름 ⑧**리소스 묶음** — `_tools/tier-res.mjs --spec <spec.json> --sheet` 가 원본(url)을 임시 폴더에 받아 헤드리스 크롬에서 **알파 경계 → zoom·focus 로 얼굴 중심 정사각 → 512² webp** 로 만들고 `manifest.json`(이름·희귀도·속성·역할·파일·크롭 기록)·`spec.json`(재현용)·`contact-sheet.png`(대조판, 커밋 안 함)을 쓰며 `res/index.json` 에 묶음을 등록한다. 첫 묶음 = **쿠키런: 크럼블 TSSR 3·SSR 23**(출처 crumblehub.co game-assets 인게임 초상 스프라이트 재게시 + 같은 사이트 코덱스 데이터, © Devsisters — 나무위키 목록 대비 미수록 2: 데빌스투스(SSR)·비겁한(TSSR) = 자산 없음). 도구의 **「리소스 불러오기」 서랍**(편집 열을 덮음: 게임 셀렉트·희귀도 칩·64px 카드 그리드)에서 **클릭 = 「넣을 티어」에 추가 / 끌어서 보드에 놓기**(문서 포인터 이벤트 + 72px 고스트, 캔버스 위에서 삽입 마커), 보드에 있는 카드는 흐리게. 리소스 항목은 `res:{b,c}` 참조로 저장돼 **새로고침·JSON 불러오기 뒤에도 그림이 복원**된다(손수 넣은 그림은 메모리만) ⑨**프리셋 삭제** — 기본 S·A·B·C(빈 슬롯 3/4/4/3) ⑩**부제 삭제** — 제목(게임 이름 30자)은 가운데 정렬 **제목 띠**(56px, 폭에 맞춰 25→15px 축소). 치수: 트레이 6 · 코어 패딩 18 · 플라크 60 · 타일 = (573 − 간격)/열 → 3열 184 / 4열 135 / 5열 106 / 6열 87, 이름 = 타일×.14(12~19). **★4차 = 작성자 시그니처 스티커(09-07 사용자 «봄딩·영도 프로필을 다른 포즈로 각자 시그니처처럼»)**: 보드 **바닥 오른쪽**에 디자인별 캐릭터 스티커 1장 — 높이 96 · 구분선 위로 34 걸침(바닥 영역 40→70) · 서명 텍스트·글리프는 그 왼쪽으로 밀린다. 자산 `_toolbox/tier/sig/<design>.webp`(봄딩 18KB·영도 23KB, 알파 webp) = **각 작성자 아바타(`_pwa-src/raw/av-<writer>.png`)를 `-Ref` 로 나노바나나 생성 → `cheer-2026-09/nb_post.py` 로 배경 키잉·트림·높이 360**. 포즈는 **축하 스플래시(봄딩 점프·영도 비행)와 겹치지 않게** 티어표 결로 잡았다 — 봄딩 = 체크리스트 클립보드+윙크(«평가하는 사람»), 영도 = 팔짱+검지 들어 설명+슬라임(«분석해 주는 사람», output-format §1-4 데이터시트 결). 「시그니처」 칩으로 끄고 켠다(기본 켬 · `design.sig` 로 저장). **자산을 못 받으면 조용히 안 그리고 서명 텍스트만 남는다**(보드는 그대로 완성). 원본·프롬프트·후보 4장 = `쓰담/docs/assets/tier-2026-09/`. ★작성자 격리: 봄딩 스티커를 영도 보드에 쓰지 않는다(디자인 전환 시 파일이 갈린다).
**★3차(09-07 사용자 «리소스가 타일과 안 맞는다·얼굴 잘림·텍스트 잘림»)**: 그림 타일은 디자인 틴트 **방사 바탕**(`tileTint` 봄딩 `#F9E4EC` / 영도 `#E3EEEA`, 가운데 흰색)을 깔고 그림을 cover 로 얹는다(투명 스프라이트가 «안 맞아» 보이던 것) · 이름은 **2줄까지**(`nameLines` — 어절 경계 우선·글자 폴백·2줄째 …, 상한 14→20자, 줄 높이는 **띠 단위로 통일** — 한 항목이라도 2줄이면 그 띠 전체 줄이 2줄 높이라 격자가 안 흔들린다) · 리소스 26종은 **디버그 시트(원본 위 크롭 상자)로 대조해 개별 zoom·focus**(얼굴이 크롭 중앙 60% 안, 머리 크기 비슷하게, `spec.json` 에 보존 · `tier-res.mjs --sheet --debug --cols 6 --cell 210`). 저장 `localStorage.sseudam_tier_v2`(v1 보드가 있으면 제목·티어·항목만 이어받음). 게이트 `_tools/verify-tier.mjs`(v2 82항목: 레일·판 폭·작업대 높이·2배 캔버스·기본 보드·디자인 2종 실색·헥스·모양 5·열·쉼표 추가·▲▼·셀렉트·**실제 마우스 드래그(스테이지 내부 스크롤 보정)**·클릭 선택·티어 추가/삭제/되돌리기·빈 티어 «+»·**리소스 서랍(목록·필터·클릭 추가·끌어 놓기·재방문·새로고침 복원)**·PNG 1386×2H·선택 링 없음·라벨 12.5·포커스 링·히트존·칩 32·탭 이탈/복귀·1366 레일 없음·390 단일 열/터치/서랍) + design-reviewer 실렌더. ★교훈: 본문 열 폭은 `--maxw 1080` 이 아니라 **1032**(패딩 48 포함)라 «판 = 본문 폭» 실측은 `.main.clientWidth − 48` 로 잰다 · 판이 넓어지면 이웃 도구(썸네일)의 `max-width` 캡이 풀려 설정 열이 1080 을 넘을 수 있다(프리셋 타일 68→56 으로 정정).
- **모바일 섬(탭 4개)**: `@media ≤860` 섬 `right:68px` 로 종 자리를 비운다(전엔 4번째 탭이 종 밑에 깔렸다). `≤400px` 는 로고·구분선을 접고 탭 패딩 10px — 숫자 달린 탭 4개가 360px 에 들어간다(실측 262/268px). 탭 줄 `.isl-tabs{overflow-x:auto}` 는 그래도 안 들어가는 폭의 폴백(알약은 내용과 같이 스크롤).
- **게이트** `_tools/verify-toolbox.mjs`(썸네일) + `_tools/verify-tier.mjs`(티어표, 09-07) — 둘 다 헤드리스 CDP · 의존성 0. 썸네일 쪽(v2.2 104항목: 탭 진입·로드·이미지 등록·타이틀 카운터·프리셋 5·긴 제목 축소·부제 경고·색·글꼴·확대·드래그·가운데·PNG 파일명·blob·미리보기 실크기 183/90/모바일 3열·Esc·포커스 복귀·탭 이탈/복귀 상태 유지·390/360 섬 넘침·종 겹침·모달 축소 배율) + design-reviewer 실렌더.

### Motion — v5.1 모션 계층 (2026-09-04 · GSAP 3.15 + Flip, `_vendor/gsap/` 로컬 벤더)

**원칙 5** — ①상태가 바뀌면 반드시 보간한다(뷰·행·제목·메타 숫자·탭 알약·모달·토스트·알림판) ②이동에는 방향이 있다(탭 순서·날짜 순서: 최신이 오른쪽)
③나갈 땐 ≤180ms `power2.in`, 들어올 땐 ≤500ms `expo.out` ④살아남는 요소는 다시 그리지 않고 제자리에서 움직인다(FLIP)
⑤GSAP 이 없거나 모션이 꺼지면 전부 CSS 폴백(`html.gs` 없음)으로 즉시 확정된다. 바운스·엘라스틱 이징은 쓰지 않는다.

- **뷰·본문 교체 `MO.swap`**: 나가기(0.16s, 방향 반대로 18px) → 내용 교체(+맨 위로) → 들어오기(0.5s). 탭 전환은 `#view`, 트렌드 날짜·검색은 `#tbody` 만(검색줄은 셸로 남아 타자 중 유지).
- **목록 행 FLIP** (`paintRows`·`archive`): 이전 자리 기록 → DOM 재배치(기존 노드 이동) → 새 자리로 0.42s. 새 행은 떠오르고(stagger ≤0.28s), 빠지는 행은 `absoluteOnLeave` 로 접힌다. 아카이브 행은 오른쪽으로 28px 밀려 나간다.
- **탭 알약**: 먼저 두 탭을 덮을 만큼 늘고(0.17s) 목적지로 수축(0.46s). 숫자가 바뀌어 폭만 달라지면 폭만 0.3s.
- **제목·메타·숫자**: 제목은 위로 빠지고 아래에서 들어온다. 메타는 구조가 같으면 숫자만 제자리에서 굴리고(0.55s), 다르면 크로스페이드 후 이전 값에서 이어 굴린다.
- **모달**: 배경(0.24s) → 판(0.5s, y18·scale.975) → 본문 항목 스태거(총 0.22s). 닫기는 0.16s. **토스트**는 flex 래퍼가 가로 중앙을 잡고 본체는 y·scale 만 움직인다(GSAP 이 `translate` 를 덮어써도 중앙이 깨지지 않게).
- **SVG 부품**: 아카이브 뚜껑(hover 들림)·검색 돋보기(focus 기울기)·페이저 화살촉(가는 방향)·종(unread 증가 시 1회 울림)·핀(추가 시 꽂힘)·섹션 아이콘 스트로크 드로잉(리빌 시)·발주 완료 체크 드로잉·로고 두 줄 쓰기(로드 1회 0.5s).
- **진입 `.rise`**: 가시성을 클래스에 묶지 않는다 — 기본이 «보임», GSAP 이 있을 때만 그리는 프레임 안에서 감췄다가 떠오른다. 화면 밖은 IntersectionObserver, 6초 백스톱.
- **로딩**: 스피너 대신 스켈레톤 행(shimmer).
- **★모션은 항상 켜져 있다 — 끄는 토글·저장 설정 없음, OS `prefers-reduced-motion` 도 따르지 않는다(사용자 결정 09-04)**: 이 PC 는 Windows 「애니메이션 효과」가 꺼져 있어 크롬이 reduce 를 보고했고, 사이트가 그걸 그대로 따라 모든 전환을 0.01ms 로 만들던 것이 «틱»의 원인이었다(09-04 실측: `SPI_GETCLIENTAREAANIMATION=false`). 처음엔 «기본 ON + 종 왼쪽 토글»로 넣었으나 사용자가 «끄는 것 자체를 없애라»고 결정해 토글·`localStorage.sseudam_motion`·reduce 규칙을 전부 제거했다. `html.gs` 는 GSAP 파일을 받았는지만 뜻한다(못 받으면 CSS 폴백).
- 성능: transform·opacity 위주(알약 폭만 예외 — 38px 소형 요소), 스크롤 리스너 대신 IO, 헤드리스 실측 rAF 간격 최대 17ms(탭 전환)·FLIP 40행 첫 프레임 50ms 1회.

### Cheer Splash — 축하 스플래시 (2026-09-04 · v2 «빵빠레» 09-05 · v2.1 «5초 판» 09-05 · 한시 운영, `_design/cheer/`)
봄딩의 네이버 메이트 선정(2026-09)을 축하하는 진입 화면. v5 가 폐기한 «부팅 스플래시»의 부활이 아니라 **한시 이벤트 레이어**다 — `index.html` 은 `<body>` 첫 줄의 `<script src="_design/cheer/cheer-splash.js">` 한 줄만 알고, 파일 하나가 스타일·마크업·모션·FX 를 스스로 넣는다(걷어낼 땐 그 한 줄 삭제).
- **길이(사용자 결정 09-05)**: 진입 ≈0.15s + `hold` 4.0s + 퇴장 0.85s ≈ **5.0초**. 데이터가 늦으면 `maxWait` 5.8s 까지 기다렸다가 걷힌다(최대 ≈6.7s). 바닥 3px 핑크 바 = hold 의 실제 경과(가짜 진행률이 아니라 최소 노출 타이머 그 자체).
- **구성**: 이중 베젤 플라크(트레이 24/코어 18) 위에 봄딩 스티커(나노바나나, 아바타 -Ref → 배경 키잉)가 위쪽 가장자리에 걸쳐 얹히고, 네이버 그린 점 키커 «네이버 메이트 · 2026년 9월» → 44px/800 헤드라인(«봄딩» 만 `--w-bomding`) → «당신의 글쓰기 동료, 쓰담 드림». 영도(비행 포즈 2장)가 플라크 옆 타원 궤도를 돈다. 색은 정체성에만(봄딩 핑크·영도 틸·네이버 그린) — v5 원칙 그대로.
- **안무(GSAP, 없으면 같은 CSS 키프레임)**: 0.0 플라크·봄딩 떠오름 → 0.3 헤드라인 글자 슬램(어절 단위 keep-all, 1.22→1 + blur 8→0) → **★0.95 빅 비트**(헤드라인이 다 앉은 순간: 화면 플래시 .55 · 플라크 뒤 블룸 · 충격파 링 핑크 280/그린 200 · 방사 버스트 · 코너 캐논 2발+스트리머+머즐 플래시 · 플라크 킥 1.04→1) → 1.15 영도 진입(궤도 2.9s/바퀴, 앞 z3·scale 1 / 뒤 z0·.78, 진행 방향 기울기, 방향 전환은 얇아졌다 펴지는 2D 턴, 0.4s 포즈 교대, 꼬리 틸 반짝이) → 1.5 봄딩 «기뻐서 뛰기» 루프(스쿼시 → 점프+점프 포즈 → 착지 스쿼시+하트) → 1.45~3.75 불꽃 7발(잔상 스파크+화이트 코어+링 2) → 1.8 «축하해요!» / 3.3 «정말 멋져요!» 말풍선(말꼬리는 영도 쪽) → 2.55 영도 배럴롤 → 2.7 2차 볼리 → 3.0 광택 2 → 4.0 퇴장. 바운스·엘라스틱 없음 — 펀치는 «크게서 작게 expo.out», 탄성은 스쿼시·스트레치로.
- **FX**: 의존성 0 의 Canvas 2D 파티클 2겹(뒤/앞 — 앞 겹은 플라크 위로도 떨어지며 **먹색은 뒤 겹에만**, 흰 판 위에선 때처럼 보인다). 컨페티는 뒤집힐 때 어두운 뒷면(양면). 종류 6: 컨페티(리본·원·하트·별)·스파크·스트리머·링·트윙클·플래시선. 상한 데스크톱 1100·모바일 550, 모바일은 수량 ≈55%.
- **퇴장**: 영도가 오른쪽 위로 이탈(0.38s) → 판이 카메라 쪽으로 밀리며 흐려짐(scale 1.04·blur 6, 0.36s) → 조리개(mask radial-gradient, 36px 페더) 0.62s. 클릭·Esc·Enter·Space·우하단 «건너뛰기» 로 즉시 종료.
- **반드시 걷힌다**: 데이터 준비(#view 스켈레톤 소멸 **+380ms** — 데이터 도착 직후 사이트 첫 렌더 버스트가 rAF 를 ~0.4s 붙들어 퇴장이 끊기는 것을 피한다) 뒤 종료 + maxWait 5.8s 백스톱 + JS 가 죽어도 CSS `cheerDead` 가 7.5s 에 강제 숨김. 가짜 진행률 없음.
- **빈도·수명**: 세션당 1회(`sessionStorage.sseudam_cheer_202609`) · `?cheer=1` 재생 · `?cheer=0` 끔 · **`CFG.until` 2026-10-04 지나면 자동 종료** — 연장·단축은 상수 하나. `window.SseudamCheer.skip()/state()`.
- 모션은 항상 켬(§Motion 결정과 동일). 검증=헤드리스 CDP **실시간** 프레임·프로브(데스크톱·390 모바일·GSAP 차단 폴백·클릭 스킵·세션 1회) — `--virtual-time-budget` 캡처는 GSAP 안무를 진행시키지 못해 쓰지 않는다.

### Boot Sequence (부팅 스플래시)
"프로그램이 켜지는 느낌" — 로고가 **그려지고**(쓴다) → **하트가 얹히고**(마음을 담는다) → 화면이 원형으로 **열린다**.

- **로고 마크**: 노트 아웃라인 드로잉(`stroke-dashoffset`) → 면 채움 → 본문 두 줄 긋기 → 하트 팝. 총 2.4초.
  마크는 스플래시 전용이며, 헤더는 워드마크만 쓴다(`.logo .mark{display:none}`).
- **빈도**: 세션당 1회(`sessionStorage.sseudam_booted`). 재방문·새로고침은 0.3초 페이드.
  `?boot=1`로 언제든 재생(모션 설정·세션 기록 무시).
- **진행 표시**: 실제 fetch 5종(posts/trend/published/hidden/mpub) 완료에 연동한다.
  **연출용 가짜 진행을 만들지 않는다** — 수치를 지어내지 않는다는 원칙의 연장.
- **★반드시 걷힌다**: 데이터가 영영 안 와도 `MAX_WAIT`(7초) 백스톱이 강제 종료한다.
  스플래시는 화면을 통째로 가리므로, 걷히지 않는 실패 모드는 곧 서비스 중단이다.

### Sound (SFX)
- Web Audio **합성**으로만 만든다 — 오디오 파일 0바이트. 부팅음(패드 + 획 노이즈 스윕 ×2 + 하트 팝 + 착지 화음), 탭 틱, 글 진입 스위시. 마스터 게인 0.14로 절제.
- **★브라우저 자동재생 정책**: 사용자 제스처 전에는 소리를 낼 수 없다. `resume()`을 시도하고 막히면 첫 포인터/키 입력에 해제하며, **실패해도 조용히 넘어간다**(에러·모달 금지).
- 우측 상단 토글 제공, 선호는 `localStorage.sseudam_sfx`에 저장.

## 6. Do's and Don'ts

### Do
- **Do** 모든 블록을 패널로 감싸 배경·테두리·여백으로 영역을 나눈다.
- **Do** 배경(`paper`)을 패널(`surface`)보다 어둡게 유지한다.
- **Do** 면과 글자를 같은 테마 쌍(`--ink`/`--paper`, `--accent`/`--on-brand`)으로 지정한다.
- **Do** UI 라벨을 12.5px 이상으로 둔다.
- **Do** 모노스페이스를 숫자에만 쓰고 `tnum`으로 자릿수를 맞춘다.
- **Do** 버튼 높이를 36~38px로 고정하고 hover/active/on 상태를 눈에 보이게 한다.
- **Do** 작성자 색 옆에 항상 이름 텍스트를 같이 둔다(색만으로 정보 전달 금지).
- **Do** 1920×1080에서 먼저 확인하고, 1366·720·390에서 되짚는다.
- **Do** 본문 대비 ≥4.5:1을 라이트/다크 양쪽에서 지킨다.
- **Do** 한국어 본문에 `word-break: keep-all`을 유지한다.
- **Do** 모든 모션에 `prefers-reduced-motion` 대체 경로를 둔다.

### Don't
- **Don't** 은유를 위해 명료함을 희생한다 — v3가 정확히 그렇게 실패했다.
- **Don't** 세리프를 본문·UI에 들인다. Pretendard 하나로 간다.
- **Don't** 10~12px 마이크로 라벨을 쓴다.
- **Don't** 얇은 선·점선만으로 영역을 나눈다.
- **Don't** `color:#fff` 같은 고정 색을 테마 토큰 면 위에 올린다(다크에서 소실된다).
- **Don't** hover에서만 나타나는 컨트롤을 만든다 — **터치 기기엔 hover가 없어 기능이 사라진다.**
  꼭 흐리게 하려면 `@media (hover:hover) and (pointer:fine)`로 가둔다.
- **Don't** 백엔드가 주지 않는 수치를 만들어 채운다. 모르면 모른다고 적는다.
- **Don't** 작성자 색을 장식으로 전용하거나 서로 교차한다.
- **Don't** 999/9999 같은 임의 z-index를 쓴다 — `--z-*` 시맨틱 스케일을 따른다.
- **Don't** 자동검증 발행글(`data-autopub="1"`)에 관리 메뉴를 노출한다 — 보호 규칙이다.
