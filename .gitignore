.claude/
# 트렌드/분석 임시파일(헬퍼·리다이렉트 산출) — 저장소에 커밋 금지
_cand_*
_fin_*
_tmp_*
out_*.json
err_*.txt

# 롤체지지 원본 캐시(재생성 가능·건당 수백 KB) — 커밋 금지.
# ★단 snapshots/ 는 «어제 대비 변동»의 유일한 근거라 커밋한다(지우면 복구 불가).
_trend/_lolchess/*.json
