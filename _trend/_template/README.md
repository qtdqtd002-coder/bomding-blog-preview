# 트렌드 브리핑 템플릿 정본 (7일 정리 대상 아님)

**2026-08-08 신설.** 그전까지 SKILL 이 템플릿 정본으로 지목하던
`_trend/겜더쿠/2026-06-24.html`·`2026-06-13.html` 은 **7일 보관 정책에 이미 삭제돼 존재하지 않았다.**
그래서 매일 "전날 발행분을 복제"하는 방식이 굳었고, 한 회차의 실수가 다음 회차로 **상속**됐다
(트렌드팀 brain STATE 에 같은 drift 가 3회 기록됨).

## 규칙
- 매일 생성은 **이 폴더의 `<작성자>.html` 을 복제**해 데이터만 갱신한다. 전날 발행분을 복제하지 않는다.
- 이 폴더는 `[A2] 7일 초과분 정리`의 **대상이 아니다** — 정리 루프는 `_trend/<작성자>/*.html` 만 돈다
  (`foreach($w in '봄딩','영도','겜더쿠','연봄','하루살이')`). `_template/` 은 그 목록에 없어 안전하다.
- 구조·CSS·탭 매핑을 **의도적으로** 바꿨을 때만 이 파일을 갱신한다(갱신 시 무엇을 왜 바꿨는지 한 줄 주석).
- 초기 스냅샷 출처 = 2026-08-08 발행분(작성자별 최신).
